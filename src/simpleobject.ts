import {
    LitElement,
    TemplateResult,
    html,
} from "lit-element";

export default class SimpleObject extends LitElement {
    protected render(): TemplateResult {
        return html``;
    }
}
